# Quick Reference: 2025-08-05-00-16

**Continue from:** Complete `mmp scrapbook` implementation plan created, ready to begin coding
**Key insight:** Existing `mmp transcribe` command works perfectly for images - reuse it via subprocess calls
**Stephen's priority:** Generalize his 4-script scrapbook workflow into reusable MMP command
**Next action:** Implement ScrapbookService.ts in /mmp/src/services/ with 4-step workflow

## Critical Files
- `/home/stephen/Documents/initial-media-processing/scrapbook-plan.md`: Complete implementation plan with TypeScript code
- `/photos/staging/ready/scrapbook/my-family-story/`: Stephen's working scripts to replicate
- `/mmp/src/cli.ts`: Where to add new scrapbook command
- `/mmp/src/services/`: Where to create ScrapbookService.ts

## Active Tasks
- **Create ScrapbookService.ts**: Implement 4-step workflow (transcribe, normalize, embed, metadata)
- **Add CLI command**: Integrate scrapbook command with proper validation and error handling
- **Test implementation**: Verify output matches original 4-script sequence exactly

## Key Implementation Details
- **Required flags:** --series and --location only
- **Default behavior:** All processing steps enabled (can be disabled with --no-* flags)  
- **Error pattern:** Use console.error() and process.exit(1) like other MMP commands
- **No `any` types:** Proper TypeScript typing throughout
- **Reuse existing:** Call `mmp transcribe` command, leverage ExifTool patterns from other commands